import React from 'react';

const ErrorView = () => {
  return (
    <div>
      <h1>
        We're sorry, but the page requested either doesn't exist or isn't available as of now.
      </h1>
    </div>
  );
};

export default ErrorView;
